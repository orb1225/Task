#coding=utf-8
import time
class Task(object):
    def __init__(self,taskname,type,**param):
    #初始化：任务名，
    # 任务类型(hive，hive2mysql)
    # 创建时间，
    # 执行状态（0：未执行，1：执行完成，2：正在执行，3：错误），
    # 优先级（越大越优先）,
    #重要性：0为不放弃，1为放弃
        self.create_time = param.get("create_time",time.strftime("%Y%m%d"))
        self.status = 0
        self.priority=param.get("priority",3)
        self.taskname=taskname
        self.type=type#hive任务或者同步任务
        self.script=param.get("script",[])
        self.importance=param.get("importance",0)
        self.parents=param.get("parents",[])
        self.childs=param.get("childs",[])

class hive_task(Task):
    def replace(self):
        script=super.script






class export_task(Task):
    pass

























































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































